# Core server (server.py, auth.py) runs on the Python standard library alone.
# These extras enable optional features and degrade gracefully if missing:
yt-dlp>=2024.1.0        # server-side YouTube audio proxy (/api/audio)
gTTS>=2.3.2             # Google Text-to-Speech for Sable's voice (/api/tts)

# Optional — uncomment to enable Sable's local neural voice (heavy, needs model):
# piper-tts             # local neural TTS (needs a voice model in persona/voices/)
google-auth
