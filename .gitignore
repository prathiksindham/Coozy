# Dependencies
node_modules/

# Secrets & credentials — never commit
persona/.env
data/.session_secret

# Runtime data (SQLite db of accounts/sessions)
data/*.db

# Large ML model binaries — re-downloadable, not source (see AGENTS.md)
models/
*.onnx

# Python
__pycache__/
*.pyc
*.pyo

# macOS / editor cruft
.DS_Store
*.swp

# Claude Code local machine settings (permissions allowlist, etc.)
.claude/settings.local.json
