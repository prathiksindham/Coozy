# All are optional — the server degrades gracefully when they're absent.
# anthropic>=0.40.0   # Anthropic Claude (needs ANTHROPIC_API_KEY) — optional
# piper-tts           # Local neural TTS voice — optional, needs model file in persona/voices/
# yt-dlp is in the main requirements.txt
